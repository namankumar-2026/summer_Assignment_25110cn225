//Program to check the entered number is strong number or not
#include<stdio.h>
#include<math.h>
int main()
{
    int n, num, originalNum, remainder, result; // Declare variables to store the number, its original value, the remainder, and the result of the calculation
    printf("Enter a number: "); // Prompt the user to enter a number
    scanf("%d", &n); // Read the number from the user

    num = n; // Set num to the entered number
    originalNum = num; // Store the original number for later comparison
    result = 0; // Initialize result to 0

    while (originalNum != 0) // Loop until all digits have been processed
    {
        remainder = originalNum % 10; // Get the last digit of the number
        result += tgamma(remainder + 1); // Add the factorial of the digit to the result using tgamma function
        originalNum /= 10; // Remove the last digit from the number
    }

    if (result == num) // Check if the calculated result is equal to the original number
        printf("%d is a strong number.", num); // If true, print that it is a strong number
    else
        printf("%d is not a strong number.", num); // If false, print that it is not a strong number

    return 0; 
}