Write a program to Print number pyramid.
    1
   121
  12321
 1234321
123454321

#include<stdio.h>
int main()
{
    int i, j, rows; // Declare variables for loop counters and number of rows
    printf("Enter the number of rows: "); // Prompt the user to enter the number of rows for the pattern
    scanf("%d", &rows); // Read the number of rows from the user

    for(i = 1; i <= rows; i++) // Outer loop to iterate through each row
    {
        for(j = 1; j <= (rows - i); j++) // Inner loop to print spaces before the numbers
        {
            printf(" "); // Print a space
        }
        for(j = 1; j <= i; j++) // Inner loop to print increasing numbers in each row
        {
            printf("%d", j); // Print the current number
        }
        for(j = (i - 1); j >= 1; j--) // Inner loop to print decreasing numbers in each row
        {
            printf("%d", j); // Print the current number
        }
        printf("\n"); // Move to the next line after printing each row
    }
    
    return 0; 
}